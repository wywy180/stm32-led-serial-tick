/******************************************************************************
 * @file    stm32f4xx_it.c
 * @brief   中断服务程序实现
 *          本工程仅用 SysTick 中断（1ms 滴答），其余异常/中断在启动文件中
 *          弱定义为死循环，需要时在此添加强定义覆盖即可。
 ******************************************************************************/
#include "stm32f4xx_it.h"
#include "bsp_tick.h"               // bsp_tick_inc()

/**
 * @brief SysTick 中断服务程序（每 1ms 触发一次）
 *        仅做毫秒计数自增，保持极短，不在此做其它工作
 */
void SysTick_Handler(void)
{
    bsp_tick_inc();                 /* g_tick++ */
}
